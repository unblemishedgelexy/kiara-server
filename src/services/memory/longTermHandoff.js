'use strict';

/**
 * Placeholder for Long Term Memory handoff.
 * This module only prepares the STM snapshot pipeline.
 */

function prepareLongTermMemory(snapshot) {
  return snapshot;
}

module.exports = {
  prepareLongTermMemory,
};
